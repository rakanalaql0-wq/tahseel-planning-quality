# منصة التخطيط والجودة العلمية — جمعية تحصيل المعرفة

تطبيق ويب عربي (RTL) يحوّل **مقياس الجودة التعليمية (300 درجة)** من ملف تشغيلي إلى نظام رقمي
متعدد الحسابات والصلاحيات، يوجّه كل مستخدم تلقائيًا إلى واجباته بحسب دوره والبرنامج والتوقيت،
ويحسب الدرجات والتغطية ويجمع الشواهد والتنبيهات والتقارير دون متابعة يدوية.

> **المبدأ الحاكم:** المستخدم لا يبحث عما يجب عليه فعله؛ النظام يعرض له واجباته، وموعدها،
> والنموذج المطلوب، ونسبة الاكتمال، والشواهد الناقصة.

هذه النسخة تنفّذ **المرحلة 1 (MVP)** من خارطة التطوير في وثيقة التحليل، مع أجزاء من المرحلة 2
(التنبيهات، الإجراءات التصحيحية، المقارنات، التصدير).

---

## فتح المنصة فورًا في المتصفح (بلا تثبيت)

اضغط الرابط التالي لتشغيل المنصة في GitHub Codespaces:

**https://codespaces.new/rakanalaql0-wq/tahseel-planning-quality/tree/claude/platform-planning-quality-hl955j**

تُهيَّأ البيئة تلقائيًا (تثبيت Node 22، وتهيئة قاعدة البيانات، وزرع برنامج تجريبي كامل)،
ثم يبدأ الخادم ويظهر إشعار بفتح الرابط على المنفذ 3000. الدخول بأي حساب من
[الحسابات التجريبية](#الحسابات-التجريبية) وكلمة المرور `Tahseel@2026`.

> **ملاحظات:**
> - الرابط **خاص بك** افتراضيًا. لمشاركته مع الفريق: بوّابة `PORTS` في الأسفل ←
>   زر يمين على المنفذ 3000 ← `Port Visibility` ← `Public`.
> - هذه بيئة **للعرض والتجربة فقط**؛ البيانات تزول عند حذف الـ Codespace.
>   للتشغيل الفعلي انشر المنصة على خادم دائم.
> - إن لم يبدأ الخادم تلقائيًا، اكتب في الطرفية: `npm start`.

---

## التشغيل محليًا

المتطلب الوحيد هو **Node.js 22.5 أو أحدث** — لا توجد أي اعتماديات خارجية
(قاعدة البيانات SQLite مدمجة في Node عبر `node:sqlite`، والواجهة والخادم مكتوبان بالكامل
بمكتبات Node القياسية).

```bash
node --version      # يجب أن يكون 22.5+
npm run seed        # تهيئة قاعدة البيانات + بنية المقياس + برنامج تجريبي
npm start           # التشغيل على http://localhost:3000
```

أوامر إضافية:

| الأمر | الوظيفة |
|---|---|
| `npm run dev` | تشغيل مع إعادة التحميل التلقائي عند التعديل |
| `npm run reset` | حذف قاعدة البيانات وإعادة التهيئة من الصفر |
| `npm run seed -- --no-demo` | تهيئة بنية المقياس والحسابات دون البرنامج التجريبي |
| `npm test` | تشغيل الاختبارات (46 اختبارًا) |

<a id="الحسابات-التجريبية"></a>
### الحسابات التجريبية

كلمة المرور الافتراضية للجميع: `Tahseel@2026` (قابلة للتغيير عبر `TPQ_SEED_PASSWORD`).

| الحساب | الدور |
|---|---|
| `admin` | مدير النظام |
| `manager` | مدير التخطيط والجودة |
| `officer` | مسؤول البرنامج |
| `supervisor` | المشرف |
| `academic` | مسؤول التخطيط والجودة العلمية |
| `quality` | مسؤول التخطيط والجودة |

### متغيرات البيئة

| المتغير | الافتراضي | الوصف |
|---|---|---|
| `PORT` | `3000` | منفذ الخادم |
| `HOST` | `0.0.0.0` | عنوان الاستماع |
| `TPQ_DB` | `data/tpq.db` | مسار قاعدة البيانات (`:memory:` للاختبارات) |
| `TPQ_DEMO` | `1` | `0` لتعطيل زرع البرنامج التجريبي عند أول تشغيل |
| `TPQ_SEED_PASSWORD` | `Tahseel@2026` | كلمة المرور الافتراضية للحسابات المزروعة |

الحد الأدنى لنسبة الاستجابة (70%) مخزّن في جدول `settings` تحت المفتاح `min_response_rate`
ويمكن تعديله دون تعديل الكود.

---

## المقياس: 300 درجة

| القسم | الدرجة | المحاور |
|---|---|---|
| جودة البيئة والمرافق | **75** | ملاءمة القاعات 20 · نظافة القاعات والمرافق 20 · تهيئة القاعات قبل الدرس 10 · التجهيزات التعليمية والتقنية 20 · الضيافة والخدمات المساندة 5 |
| جودة العملية التعليمية | **135** | كفاءة المعلم وجودة المحتوى 50 · الالتزام بالخطة التعليمية 35 · الأنشطة والتطبيقات الإثرائية 20 · المتابعة والانضباط التعليمي 30 |
| جودة الدعم وتجربة الطالب | **90** | معالجة الشكاوى 15 · الدعم والإرشاد والتواصل 30 · الرضا والانتماء 25 · استمرارية الطلاب 20 |

### تقييم المعلم من مصدرين

محور «كفاءة المعلم وجودة المحتوى العلمي» (50 درجة) موزّع على أربعة مؤشرات:

| المؤشر | الدرجة | الأداة |
|---|---:|---|
| مناسبة المعلم للمادة | 10 | قائمة تحقق قبل البدء |
| تمكن المعلم وجودة تقديمه — **استبانة الطلاب** | 10 | استبانة (منتصف ونهاية) |
| تمكن المعلم وجودة تقديمه — **الزيارة الصفية** | 10 | قائمة تحقق، **مرتان على الأقل**، والدرجة متوسط الزيارتين |
| جودة وسلامة ومناسبة المحتوى العلمي | 20 | قائمة تحقق قبل البدء |

هذا يمنع الحكم على المعلم من مصدر واحد: رأي الطلاب يقيس التجربة، والزيارة الصفية
يقيسها مسؤول التخطيط والجودة العلمية مهنيًا.

24 مؤشرًا و77 عنصر تحقق و25 سؤال استبانة. **الأوزان والدوريات وخطط العينة كلها قابلة للتعديل
من شاشة «إدارة المقياس» دون تعديل الكود**، والشاشة تحذّر فورًا إذا لم يعد المجموع 300.

---

## قاعدة «غير منطبق»

ليست كل المؤشرات منطبقة على كل برنامج: برنامج بلا ضيافة، أو برنامج عن بُعد بلا قاعات.
يستطيع **مدير التخطيط والجودة وحده** وسم مؤشر «غير منطبق» على برنامج معيّن بسبب إلزامي موثّق،
فيخرج وزنه من المقياس ومن اكتمال القياس، ولا يظهر كقياس ناقص ولا يمنع الإقفال.

ثلاثة ضوابط تمنع إساءة استخدام القاعدة:

1. **الصلاحية محصورة** بمدير التخطيط والجودة — لا يستطيع مسؤول البرنامج استثناء مؤشراته الضعيفة.
2. **السبب إلزامي** ويُسجَّل في سجل التدقيق باسم من وسمه وتاريخه.
3. **لا يُستثنى مؤشر له قياسات معتمدة** — النظام يرفض ذلك حتى لا تختفي نتيجة قائمة بلا أثر.

ولأن الوزن المنطبق يصبح أقل من 300، يعرض النظام رقمين:

| الرقم | المعنى |
|---|---|
| **الدرجة المحققة** | من الوزن المنطبق فعلًا على هذا البرنامج (مثال: 27.8 / 295) |
| **الدرجة المعيارية من 300** | المحقق ÷ الوزن المنطبق × 300 — تُستخدم في مقارنة البرامج المختلفة الاستثناءات |

شاشة المقارنة في التقارير تعتمد **المعيارية** حتى تبقى المقارنة عادلة.

---

## قياس الأثر — تبويب مستقل خارج الـ300

مقياس الـ300 يقيس **جودة العملية**؛ وحدة قياس الأثر تجيب عن سؤال مختلف: **هل تعلّم الطالب فعلًا؟**
ولذلك فُصلت تمامًا: لا تدخل في الدرجة، ولا في اكتمال القياس، ولا يظهر أي مؤشر أثر داخل شجرة المقياس
(ويوجد اختبار يمنع أي تداخل بينهما).

تُعرَّف أدوات القياس (اختبار قبلي، اختبار بعدي، تقييم عملي) بدرجة عظمى وحد إتقان، ثم تُدخل درجات
الطلاب، فيحسب النظام:

| المؤشر | المعنى |
|---|---|
| متوسط القبلي / البعدي | مستوى الطلاب قبل البرنامج وبعده |
| الفرق | التحسن بالنقاط المئوية |
| **مكسب التعلم المعياري** | (بعدي − قبلي) ÷ (100 − قبلي) — كم تحقق من التحسن *الممكن*، فلا يُظلم برنامج بدأ طلابه بمستوى مرتفع |
| نسبة الإتقان | نسبة من بلغوا حد الإتقان في القياس البعدي/العملي |
| نسبة من تحسّن مستواه | من الطلاب الذين قيسوا قبليًا وبعديًا معًا |
| تغطية القياس | كم طالبًا قيس من المستحقين |

للتبويب تصدير Excel مستقل، ويظهر في تقرير البرنامج كقسم منفصل موسوم بأنه خارج الـ300.

### قواعد الأعمال المنفّذة

| الرمز | القاعدة | موضع التنفيذ |
|---|---|---|
| BR-01 | إجمالي المقياس 300 = 75 + 135 + 90 | `src/db/framework.js` + تحقق في `/admin/metric` |
| BR-02 | متحقق 100% · جزئي 50% · غير متحقق 0% | `src/routes/dashboard.js` (نموذج التحقق) |
| BR-03 | الاستبانة: (المتوسط − 1) ÷ 4 × 100 | `likertToPct` في `src/lib/scoring.js` |
| BR-04 | الملاحظة إلزامية عند جزئي أو غير متحقق | تحقق في الخادم + المتصفح (`public/app.js`) |
| BR-05 | تهيئة القاعات: عينة ≥ 50% من اللقاءات | `periodicity: per_session_sample, sample_pct: 50` |
| BR-06 | الضيافة: عينة ≥ 25% وبحد أدنى مرتين | `sample_pct: 25, min_count: 2` |
| BR-07 | التجهيزات: مرتان على الأقل | `periodicity: fixed_count, min_count: 2` |
| BR-08 | الدعم والتواصل: المنتصف والنهاية | `periodicity: mid_end` |
| BR-09 | فاعلية النشاط الرئيس بعد كل نشاط | `scheduleMainActivityTask` في `src/lib/scheduler.js` |
| BR-10 | الاستمرارية: 5 رغبة + 15 فعلي | مؤشرا `I-3.4.1` و`I-3.4.2` |
| BR-11 | «اكتمال القياس» مستقل عن «نتيجة الجودة» | `computeProgram` يعيد `coverage_pct` و`quality_pct` منفصلين |
| BR-12 | لا يحكم الطالب على السلامة العلمية | أسئلة الاستبانات تُبنى من مؤشرات `tool = survey` فقط |
| BR-13 | **الاستبانة دون 70% استجابة تُوسم «عينة غير كافية»** ولا تُحتسب ضمن اكتمال القياس | `minResponseRate()` في `src/lib/scoring.js` |
| BR-14 | **رابط الاستبانة فردي لكل طالب ويُستخدم مرة واحدة** | جدول `survey_invites` ومسار `/r/:token` |
| BR-15 | **«غير منطبق»**: وزن المؤشر المستثنى يخرج من المقياس ومن اكتمال القياس، بسبب موثّق | جدول `indicator_exemptions` + `exemptionsFor()` |

---

## الأدوار والصلاحيات

خمسة أدوار تشغيلية تُسند **داخل كل برنامج على حدة**، بالإضافة إلى صلاحيتين عامتين
(مدير النظام، مدير التخطيط والجودة). كل دور يرى مهامه المرتبطة به فقط، والحقول تظهر
حسب الصلاحية.

| الدور | أبرز صلاحياته |
|---|---|
| مسؤول البرنامج | بيانات البرنامج واللقاءات والطلاب، الحضور، الشكاوى، الانضباط، الاستمرارية، الإقفال |
| المشرف | قوائم تحقق القاعات والمرافق والتجهيزات، الشواهد، الإجراءات التصحيحية |
| مسؤول التخطيط والجودة العلمية | مناسبة المعلم، سلامة المحتوى، الخطة، تنفيذ المقرر، الأنشطة، تقييم المعلم، **الزيارة الصفية**، **قياس الأثر** |
| مسؤول التخطيط والجودة | استبانات الدعم والرضا، تحليل تجربة الطالب |
| مدير التخطيط والجودة | إنشاء البرامج وإسناد الأدوار، التحقق النهائي من الشكاوى، **وسم المؤشرات «غير منطبق»**، اللوحات والتقارير، سجل التدقيق |

---

## الوحدات المنفّذة

- **إدارة البرامج** — الفترة، الموقع، اللقاءات، الطلاب، المعلمون، الخطة، إسناد الأدوار.
- **إدارة المقياس** — أقسام ومحاور ومؤشرات وأوزان وأدوات ودوريات وقواعد حساب قابلة للإعداد.
- **«غير منطبق»** — استثناء مؤشر من برنامج بسبب موثّق، مع درجة معيارية من 300 تحفظ عدالة المقارنة.
- **الجدولة الآلية** — توليد مهام القياس من الدوريات وخطط العينة مع توزيع العينة بانتظام على اللقاءات.
- **قوائم التحقق** — 100/50/0 مع إلزام الملاحظة والشواهد وتوليد الإجراء التصحيحي تلقائيًا.
- **الاستبانات** — توليد من مكتبة الأسئلة، **رابط فردي لكل طالب يُستخدم مرة واحدة**، نتائج فورية وفق BR-03،
  ووسم «عينة غير كافية» لكل استبانة دون 70% استجابة.
- **قياس الأثر** — أدوات قبلية/بعدية/عملية، مكسب التعلم المعياري ونسبة الإتقان، مستقلة عن الـ300.
- **المتابعة والانضباط** — الحضور لكل لقاء، الحالات المتعثرة، الإجراءات، **جلسات المتابعة الموثّقة**، قنوات التواصل.
- **الشكاوى والمقترحات** — مدة معالجة (SLA)، حالة، إغلاق موثّق، تحقق نهائي من مدير الجودة.
- **الاستمرارية** — أعداد البداية والنهاية، الانسحابات وأسبابها، نية الاستمرار.
- **الإجراءات التصحيحية والتحسينية** — لوحة كانبان بأربع حالات.
- **الشواهد** — ملفات (10 ميجابايت كحد أقصى، أنواع محددة) أو روابط، مرتبطة بأي سجل.
- **الإشعارات** — قبل الاستحقاق، عند التأخر، عند نقص العينة، وعند وجود عنصر غير متحقق.
- **التقارير** — تقرير برنامج كامل قابل للطباعة PDF، تصدير Excel (CSV بترميز عربي)، مقارنة البرامج والفترات.
- **سجل التدقيق** — من فعل ماذا ومتى مع صورة قبل/بعد لكل تعديل جوهري.
- **إقفال البرنامج** — لا يتم قبل عرض القياسات الناقصة والشكاوى المفتوحة بوضوح.

---

## بنية المشروع

```
.devcontainer/           إعداد GitHub Codespaces (تشغيل بنقرة واحدة)
src/
  server.js              الخادم والموجّه وسياق الطلب
  db/
    schema.sql           مخطط قاعدة البيانات (23 جدولًا)
    framework.js         بنية المقياس 300 درجة ومكتبة الأسئلة
    seed.js              التهيئة الأولية والبرنامج التجريبي
    index.js             الاتصال بقاعدة البيانات والمعاملات
  lib/
    scoring.js           محرك الحساب: الدرجات والتغطية وقواعد BR وكفاية العينة
    impact.js            محرك قياس الأثر (مستقل عن الـ300)
    scheduler.js         توليد المهام والعينات والتنبيهات وفحص جاهزية الإقفال
    roles.js             الأدوار والمسؤوليات ومصفوفة الصلاحيات
    auth.js              كلمات المرور (scrypt) والجلسات والوصول للبرامج
    audit.js             سجل التدقيق
    http.js              قراءة الطلب، multipart، الكوكيز، الموجّه
    util.js              أدوات مساعدة (تواريخ، ترميز HTML، CSV)
  routes/                مسارات الشاشات (دخول، لوحة، برامج، عمليات، استبانات، شواهد، تقارير، إدارة)
  views/                 قالب الصفحة ومكوّنات الواجهة
public/                  app.css + app.js
test/                    46 اختبارًا (قواعد الأعمال + «غير منطبق» + قياس الأثر + رحلة HTTP)
docs/                    مطابقة الوثيقة بالتنفيذ وملاحظات التشغيل
```

---

## الأمان

- كلمات المرور بخوارزمية **scrypt** بملح فريد لكل مستخدم ومقارنة ثابتة الزمن.
- جلسات برموز عشوائية (32 بايت) في كوكي `HttpOnly` + `SameSite=Lax` بصلاحية 7 أيام.
- كل استعلامات قاعدة البيانات **مُعامَلة (parameterized)** — لا تركيب نصي لجمل SQL.
- ترميز HTML لكل مخرجات المستخدم عبر `esc()` لمنع XSS.
- تحقق من الصلاحية على مستوى البرنامج والدور في كل مسار كتابة.
- رفع الملفات مقيّد بالنوع والحجم، ويُخزَّن باسم عشوائي خارج المسار العام.
- سجل تدقيق لكل تعديل جوهري مع صورة قبل/بعد.

- خلف وسيط HTTPS (Nginx أو Codespaces) يقرأ النظام `X-Forwarded-Proto`، فيولّد الروابط
  المطلقة بـ `https` ويضيف راية `Secure` لكوكي الجلسة تلقائيًا.

**قبل التشغيل الفعلي:** غيّر كلمات المرور الافتراضية، وشغّل الخدمة خلف HTTPS،
وفعّل نسخًا احتياطيًا دوريًا لملف `data/tpq.db`.

---

## الاختبارات

```bash
npm test
```

تغطي 46 اختبارًا: قواعد الأعمال BR-01 إلى BR-15، بنية المحاور والأوزان، توزيع العينة، توليد المهام،
كفاية عينة الاستبانة عند حد 70%، قاعدة «غير منطبق» والدرجة المعيارية، جلسات المتابعة،
حسابات قياس الأثر وفصله عن الـ300، وكذلك رحلة
HTTP كاملة (تسجيل الدخول، عزل الأدوار، رفض الصلاحيات، تعبئة قائمة تحقق، دورة استبانة كاملة
بروابط فردية مع منع التكرار، وسم مؤشر «غير منطبق» ورفضه لمؤشر له قياسات، إضافة أداة أثر
وإدخال درجاتها، منع الإقفال مع وجود نواقص، والتصدير).
